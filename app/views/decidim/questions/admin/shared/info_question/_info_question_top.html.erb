<div class="card-divider">
  <h2 class="card-title">
    <%= link_to "#{t ".questions"} > ", questions_path %>
    <% if question %>
      <%= link_to present(question).reference, question.url %>
    <% else %>
      <%= t(".new_question") %>
    <% end %>
  </h2>
</div>
